To run the program, compile tsm.c using openmp.  Then run the executable with the input file name as the second command line argument.

The input file is a square matrix representing the distances between each city as an integer.
